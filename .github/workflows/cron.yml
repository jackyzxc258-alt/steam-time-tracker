name: Steam Time Collector

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Steam collection
        run: |
          curl -s -L "${{ secrets.VERCEL_URL }}/api/collect"
